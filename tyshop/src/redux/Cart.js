import * as ActionType from './ActionTypes'

export const Cart = (state = {lesProduitsInCart : [] ,nbProduit : 0},action) => {
    switch(action.type){
        default:
            return state
    }
}